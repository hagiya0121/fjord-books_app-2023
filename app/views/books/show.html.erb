<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('books.book_edit'), edit_book_path(@book) %> |
  <%= link_to t('books.book_index'), books_path %>

  <%= button_to t('books.book_destroy'), @book, method: :delete %>
</div>
